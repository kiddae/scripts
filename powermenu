#!/bin/bash
choices="Logout\nShutdown\nReboot"
chosen=$(echo -e $choices | rofi -dmenu -i -p "Power Menu" -theme ~/.colors/gruvbox/gruvbox-rofi.rasi)
echo "$chosen"

case "$chosen" in
		Logout) i3-msg exit || openbox --exit || bspc quit ;;
		Shutdown) poweroff ;;
		Reboot) reboot ;; 
esac
