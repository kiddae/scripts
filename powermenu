#!/bin/bash
choices="Logout\nShutdown\nReboot"
chosen=$(echo -e $choices | rofi -dmenu -i -p "Power Menu" -theme ~/.cache/colorer/rofi.rasi)
echo "$chosen"

case "$chosen" in
		Logout) bspc quit ;;
		Shutdown) poweroff ;;
		Reboot) reboot ;; 
esac
